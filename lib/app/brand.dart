import 'package:flutter/material.dart';

/// Единственный файл, отличающийся между приложениями конвейера.
/// Сгенерирован _pipeline/generate_v2.ps1 — править вручную нет смысла.

const String kAppTitle = 'Bova';
const String kProductTitle = 'Roster Board';
const String kProductTagline = 'Depth chart at a glance';
const String kBrandLetter = '';

/// Тема продукта: gridiron / rosterboard. Бренд: Bovada.
const bool kIsLight = true;
const double kRadius = 12;

const String kDisplayFont = 'AppDisplay';
const String kBodyFont = 'AppDisplay';

const Color cBg = Color(0xFFFBFCFD);
const Color cBgDeep = Color(0xFFE2E5EA);
const Color cSurface = Color(0xFFEFF1F5);
const Color cEdge = Color(0xFFE2E5EA);
const Color cAlt = Color(0xFF334155);

/// Акцент — от бренда, поэтому иконка перекликается с интерфейсом.
const Color cAccent = Color(0xFFD60000);
const Color cAccent2 = Color(0xFFFF1C38);